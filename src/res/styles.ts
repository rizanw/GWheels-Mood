import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  faceSquare: {
    borderColor: "green",
    borderWidth: 2,
    position: "absolute",
    width: 10,
    height: 10,             
  },
});
